# AGENTS.md

## 基本原则

在本仓库中进行开发时，始终遵循以下原则：

1. 修改前先理解。
2. 实现前先规划。
3. 优先进行最小、完整、可验证的修改。
4. 不修改与当前任务无关的代码。
5. 每完成一个逻辑单元，都应进行相应验证。
6. Commit 前必须检查完整 diff。
7. 不得破坏用户已有的未提交工作。
8. 没有验证证据，不得宣称任务已经完成。

---

## 开发流程

对于非简单修改，按照以下顺序开展工作：

```text
理解需求
→ 检查相关代码
→ 明确修改范围
→ 制定实现计划
→ 实现一个逻辑单元
→ 执行针对性验证
→ 检查 diff
→ 继续下一个逻辑单元
→ 执行完整验证
→ 最终检查
→ Commit
```

不要在尚未理解相关代码和约束的情况下直接开始修改。

如果存在可能推翻当前方案的高风险假设，应优先验证该假设，再继续开发外围功能。

---

## 修改范围

所有修改必须严格服务于当前任务。

除非当前任务明确需要，否则不要：

* 重构无关模块；
* 重命名无关变量、函数、类型或文件；
* 调整无关代码格式；
* 升级无关依赖；
* 修改无关配置；
* 引入新的抽象层；
* 修改 public API；
* 清理历史遗留代码；
* 顺手修复其他问题。

如果发现当前任务之外的问题，应记录或报告，而不是自动扩大任务范围。

优先复用项目已有的：

* 代码风格；
* 工具函数；
* 抽象；
* 目录结构；
* 测试方式；
* 错误处理模式。

在新增实现之前，先确认仓库中是否已经存在可复用方案。

---

## 修改颗粒度

修改应保持小而完整。

一个合理的修改单元应当：

* 只表达一个明确的工程意图；
* 修改最小必要范围；
* 能够独立理解；
* 能够独立 Review；
* 有明确的验证方法。

优先采用：

```text
小修改
→ 验证
→ 稳定检查点
→ 下一个修改
```

避免：

```text
一次修改大量模块
→ 最后统一测试
→ 同时处理多个相互影响的问题
```

如果一组修改无法用一两句话准确解释，应考虑进一步拆分。


## Commit 颗粒度

一个 Commit 应表达一个**完整、单一、可解释的工程意图**。

不要按照：

* 文件数量；
* 开发时间；
* Agent 执行轮次；

机械拆分 Commit。

也不要把多个逻辑上独立的修改塞进同一个 Commit。

良好的 Commit 应满足：

* 可以用一句简短的信息准确描述；
* 单独查看时具有完整意义；
* 不包含无关修改；
* 对应修改已经完成必要验证。

commit 信息用中文


